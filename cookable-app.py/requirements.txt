# Cookable App - Python Dependencies
#
# This project uses UV as the package manager
# This requirements.txt is for pip users
# To install with UV:
#   uv add streamlit scikit-learn pandas numpy
# To install with pip (fallback):
#   pip install -r requirements.txt

streamlit>=1.28.0
scikit-learn>=1.3.0
pandas>=2.0.0
numpy>=1.24.0

# Note: We got help form a friend who's a software engineer to install a working environment
